import DashboardLayout from "@/components/DashboardLayout";
import { Button } from "@/components/ui/button";
import { trpc } from "@/lib/trpc";
import { ArrowRight, BookOpen, Clock3, PlayCircle } from "lucide-react";
import { useState } from "react";

export default function Lessons() {
  const query = trpc.learning.listLessons.useQuery();
  const [selected, setSelected] = useState<number | null>(null);
  const active = query.data?.find(item => item.id === selected) || query.data?.[0];
  return <DashboardLayout eyebrow="Lộ trình của bạn" title="Bài học" description="Những nội dung được chọn lọc để bạn học theo từng bước."><div className="mx-auto max-w-7xl"><div className="mb-8 flex flex-col justify-between gap-4 md:flex-row md:items-end"><div><p className="max-w-xl text-sm leading-6 text-slate-500">Mỗi bài học là một viên gạch nhỏ. Hãy chọn một chủ đề và dành một khoảng thời gian tập trung cho nó.</p></div><div className="flex items-center gap-2 rounded-full bg-[#dff1e9] px-4 py-2 text-xs font-bold text-[#398266]"><BookOpen size={15} /> {query.data?.length || 0} bài học</div></div><div className="grid gap-6 lg:grid-cols-[1fr_1.05fr]"> <div className="space-y-3">{query.isLoading && [1,2,3].map(i => <div key={i} className="h-28 animate-pulse rounded-2xl bg-white" />)}{query.data?.map((lesson, index) => <button key={lesson.id} onClick={() => setSelected(lesson.id)} className={`w-full rounded-2xl border p-5 text-left transition-all ${active?.id === lesson.id ? "border-[#b8d9cd] bg-white shadow-[0_10px_30px_rgba(22,52,84,0.06)]" : "border-[#dbe3e0] bg-white/60 hover:bg-white"}`}><div className="flex items-start gap-4"><div className={`flex h-11 w-11 shrink-0 items-center justify-center rounded-xl text-sm font-bold ${active?.id === lesson.id ? "bg-[#163454] text-white" : "bg-[#e8f1ed] text-[#398266]"}`}>0{index + 1}</div><div className="min-w-0 flex-1"><div className="flex items-start justify-between gap-3"><h3 className="font-display text-lg font-bold">{lesson.title}</h3><ArrowRight size={17} className="mt-1 shrink-0 text-slate-300" /></div><p className="mt-1 line-clamp-2 text-sm leading-6 text-slate-500">{lesson.summary || "Bài học mới trong lộ trình ZEdu."}</p><div className="mt-3 flex items-center gap-3 text-xs font-semibold text-slate-400"><span className="flex items-center gap-1"><Clock3 size={14} /> {lesson.durationMinutes} phút</span><span className="flex items-center gap-1"><PlayCircle size={14} /> Nội dung đọc</span></div></div></div></button>)}{!query.isLoading && !query.data?.length && <div className="rounded-2xl bg-white p-10 text-center text-sm text-slate-400">Chưa có bài học được công bố.</div>}</div><article className="min-h-[420px] rounded-[28px] bg-[#163454] p-7 text-white md:p-9">{active ? <><div className="flex items-center gap-2 text-xs font-bold uppercase tracking-[0.18em] text-[#a9d7c6]"><PlayCircle size={15} /> Bài học đang chọn</div><h2 className="mt-5 font-display text-3xl font-bold leading-tight md:text-4xl">{active.title}</h2><p className="mt-4 text-sm leading-7 text-white/65">{active.summary || "Hãy bắt đầu từ phần nội dung chính và ghi lại những điểm bạn muốn hỏi."}</p><div className="my-8 h-px bg-white/10"/><div className="prose prose-invert max-w-none text-sm leading-7 text-white/75"><p>{active.content || "Nội dung chi tiết sẽ được cập nhật bởi quản trị viên. Trong lúc chờ, bạn có thể xem phần tóm tắt và chuẩn bị câu hỏi cho buổi học tiếp theo."}</p></div><Button className="mt-8 rounded-xl bg-[#f4c8bd] text-[#163454] hover:bg-[#f7d8d0]">Đánh dấu đang học <ArrowRight size={16} /></Button></> : <div className="flex h-full min-h-[360px] items-center justify-center text-center"><div><BookOpen className="mx-auto mb-4 text-[#a9d7c6]" size={32} /><p className="text-sm text-white/60">Chọn một bài học để bắt đầu.</p></div></div>}</article></div></div></DashboardLayout>;
}
